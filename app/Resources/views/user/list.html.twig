{% extends '::base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="col-sm-12">
            <div class="row">
                {% for user in users %}
                    {{ user.name }} <br/>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}